def lastSubstring(s):
    for i in range(1, len(s)):
        print(s[i:], s[0:])
        if s[i:] > s[0:]:
            print(s[i:], s[0:])
            s = s[i:]
    print(s)
    return s


    

print(lastSubstring("cacacb"))   # "cb"